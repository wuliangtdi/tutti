{
  "schemaVersion": "tutti.app.cli.v1",
  "scope": "demo",
  "commands": [
    {
      "path": ["ping"],
      "summary": "Ping the demo app",
      "description": "Return a small JSON response from the running demo app.",
      "inputSchema": {
        "type": "object",
        "properties": {}
      },
      "output": {
        "defaultMode": "json",
        "json": true
      },
      "handler": {
        "kind": "http",
        "method": "POST",
        "path": "/tutti/cli/ping",
        "timeoutMs": 30000
      }
    }
  ]
}
