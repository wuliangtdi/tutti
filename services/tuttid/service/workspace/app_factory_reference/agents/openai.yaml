interface:
  display_name: "Tutti Workspace App Factory"
  short_description: "Create Tutti app packages or Load unpacked dev apps"
  default_prompt: "Use $tutti-workspace-app-factory to adapt this repository into a Tutti workspace app package or a Load unpacked local debug app under .tutti/dev-app/."

policy:
  allow_implicit_invocation: true
