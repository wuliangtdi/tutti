---
"@tutti-os/agent-gui": minor
---

Replace external dropped-file resolvers with the host-owned `prepareExternalPromptFiles` contract. Prepared files now require a provider-readable path or URL, while failures return typed error codes.
