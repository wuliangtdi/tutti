# PR2 Skills（cwd 感知修復 + 設置頁只讀視圖）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修復 composer options 緩存不感知 cwd 變化導致的 skills 過期問題；在工作區設置頁新增按 provider 的 Skills 只讀視圖。

**Architecture:** 緩存修復在 activity-core controller 內部加 per-provider 的 cwd 記錄（不改 snapshot 類型）：cache 命中要求 cwd 一致，否則視同 force 重取。設置頁視圖純前端：復用既有 `getAgentProviderComposerOptions` 客戶端調用取 `skills`，按 provider 分組只讀展示（name/trigger/description/sourceKind/pluginName），零新後端接口。

**Tech Stack:** TypeScript（packages/agent/activity-core 用 node:test；apps/desktop 用 vitest + React）。

**分支:** 繼續在 `capability-negotiation`（與 PR0 同分支堆疊,最終可拆 PR）。

---

### Task A: composer options 緩存的 cwd 感知

**Files:**

- Modify: `packages/agent/activity-core/src/controller.ts`（`loadComposerOptions`，:134-194）
- Test: `packages/agent/activity-core/src/controller.test.ts`（沿用現有 fake adapter 模式）

- [x] **Step 1: 寫失敗測試**（在 controller.test.ts 中,模仿既有 loadComposerOptions 測試的 setup,fake adapter 統計調用次數與收到的 cwd）

```ts
test("loadComposerOptions refetches when cwd changes and caches per cwd", async () => {
  // setup: createAgentActivityController with fake adapter whose
  // loadComposerOptions records calls and returns minimal options
  const first = await controller.loadComposerOptions({
    provider: "codex",
    cwd: "/repo/a"
  });
  const cachedSame = await controller.loadComposerOptions({
    provider: "codex",
    cwd: "/repo/a"
  });
  assert.equal(adapterCalls.length, 1); // same cwd → cache hit
  const afterSwitch = await controller.loadComposerOptions({
    provider: "codex",
    cwd: "/repo/b"
  });
  assert.equal(adapterCalls.length, 2); // cwd changed → refetch
  assert.equal(adapterCalls[1].cwd, "/repo/b");
  void first;
  void cachedSame;
  void afterSwitch;
});
```

（具體 setup 代碼以該文件既有測試為準——找到現有調用 `loadComposerOptions` 的測試,複製其 controller/adapter 構造。若無,構造最小 fake adapter:其餘方法拋錯即可。）

- [x] **Step 2: 跑測試確認失敗**

Run: `cd packages/agent/activity-core && pnpm test`
Expected: 新測試 FAIL（第二次 cwd 變化仍命中緩存,調用數=1）

- [x] **Step 3: 實現**

`controller.ts` 的 controller 閉包內（鄰近 `activeComposerOptionsLoads`）新增：

```ts
const composerOptionsCwdByProvider = new Map<string, string>();
const normalizeComposerCwd = (cwd: string | null | undefined): string =>
  (cwd ?? "").trim();
```

`loadComposerOptions` 中：

```ts
const requestedCwd = normalizeComposerCwd(input.cwd);
if (!input.force) {
  const cached = snapshot.composerOptionsByProvider?.[provider];
  if (cached && composerOptionsCwdByProvider.get(provider) === requestedCwd) {
    return cloneAgentActivityComposerOptions(cached);
  }
}
const existingLoad = activeComposerOptionsLoads.get(provider);
if (
  existingLoad &&
  !input.force &&
  composerOptionsCwdByProvider.get(provider) === requestedCwd
) {
  return existingLoad.then(cloneAgentActivityComposerOptions);
}
```

成功寫入 snapshot 的 `.then` 內（版本校驗通過後）：

```ts
composerOptionsCwdByProvider.set(provider, requestedCwd);
```

注意：in-flight 去重在 cwd 不同時不能複用舊 load——上面的條件已涵蓋；load 版本機制不變。

- [x] **Step 4: 跑包測試 + typecheck**

Run: `pnpm test && pnpm typecheck`
Expected: 全 PASS

- [x] **Step 5: Commit**

```bash
git add packages/agent/activity-core/
git commit -m "fix(activity-core): composer options cache is cwd-aware"
```

### Task B: 設置頁 Skills 只讀視圖

**Files:**

- Modify: `apps/desktop/src/renderer/src/features/workspace-workbench/ui/WorkspaceSettingsPanel.tsx`（tab 列表 :142-178 增加 "agent" tab 與渲染分支）
- Create: `apps/desktop/src/renderer/src/features/workspace-workbench/ui/WorkspaceAgentSkillsSettings.tsx`
- Modify: i18n 資源（grep `workspace.settings.tabs` 所在的 locale 文件,補 zh-CN/zh-TW/en 三份："Agent"、"技能（只讀）"、"來源"、空態文案、加載失敗文案）
- Test: `apps/desktop` 的 vitest（若該目錄組件有測試慣例則加渲染測試;若無慣例,以 typecheck + 現有 lint 為準,並在 Step 5 說明）

**數據獲取要求（實現前先勘察,遵循現有模式）:**

- grep `getAgentProviderComposerOptions` 在 apps/desktop 的調用（`desktopAgentActivityAdapter.ts:57-70` 是參考）,找到 renderer 拿 nextopd client 的注入方式（service/context）。
- 組件掛載時對 providers `["codex", "claude-code"]` 各調一次（cwd 用當前 workspace 根路徑——grep 設置面板或其容器內 workspace 對象的字段,如 `workspace.rootPath`/`cwd`;若設置面板拿不到 cwd,允許省略 cwd 參數,後端會退回 personal/system 範圍的 skills,並在 UI 註明「項目級 skills 以會話內為準」）。
- 並發 fetch、單獨容錯:一個 provider 失敗只影響該分組（顯示失敗文案）。

**UI 要求:**

- 每 provider 一個分組（標題用現有 `AGENT_PROVIDER_LABEL`）;列表項:`trigger`（代碼字體）+ `name` + `description`（次要色,單行截斷）+ `sourceKind` 徽標（plugin 時顯示 `pluginName`）。
- 空態:「未發現技能」;只讀,無任何操作按鈕。
- 樣式遵循該面板現有 section 的 class/組件慣例（先讀同文件其他 tab 的實現再寫）。

- [x] **Step 1: 勘察並記錄**（client 注入方式、cwd 可達性、i18n 文件路徑——寫進實現備註）
- [x] **Step 2: 實現組件與 tab 接線**
- [x] **Step 3: i18n 三語文案**
- [x] **Step 4: 驗證**

Run: `cd apps/desktop && pnpm typecheck`（script 名以 package.json 為準）+ 若有既有組件測試慣例則補渲染測試並跑 vitest;`pnpm lint:ts` 對改動文件無告警。
Expected: 全 PASS

- [x] **Step 5: Commit**

```bash
git add apps/desktop/
git commit -m "feat(desktop): read-only agent skills view in workspace settings"
```

### Task C: 收尾驗證

- [ ] **Step 1:** `cd packages/agent/activity-core && pnpm test && cd ../gui && pnpm vitest run`（確認無回歸）
- [ ] **Step 2:** `pnpm lint:ts 2>&1 | tail -3`（無 error）
